'use client'
import {useState} from 'react'
export default function Alerts(){const [alerts,setAlerts]=useState([['NIFTY 50','Crosses 25,500','Price'],['TCS','Moves more than 3%','Volatility'],['RELIANCE','New company news','News']]);const [on,setOn]=useState<Record<string,boolean>>({});return <main className="p-6 md:p-10 max-w-5xl mx-auto"><a href="/" className="text-sm text-cyan-400">← MarketOS</a><div className="mt-8"><div className="text-cyan-400 text-sm">ALERTS</div><h1 className="text-4xl font-bold mt-2">Market Alerts</h1><p className="text-slate-500 mt-2">Demo alerts · notification delivery comes with the live backend.</p></div><div className="space-y-3 mt-8">{alerts.map(([a,b,c])=><div className="glass rounded-2xl p-5 flex items-center justify-between" key={a}><div><div className="font-semibold">{a}</div><div className="text-sm text-slate-400 mt-1">{b} · {c}</div></div><button onClick={()=>setOn({...on,[a]:!on[a]})} className={`px-4 py-2 rounded-xl text-sm ${on[a]?'bg-emerald-400 text-black':'bg-white/5 text-slate-400'}`}>{on[a]?'ON':'OFF'}</button></div>)}</div><button onClick={()=>setAlerts([...alerts,['New alert','Custom condition','Demo']])} className="mt-5 bg-cyan-400 text-black font-semibold px-5 py-3 rounded-xl">+ Create alert</button></main>}
