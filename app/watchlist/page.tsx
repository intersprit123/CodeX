'use client'
import {useState} from 'react'
const stocks=[['TCS','₹3,284.40','+1.82%'],['RELIANCE','₹1,418.30','+2.84%'],['INFY','₹1,492.20','+1.72%'],['HDFCBANK','₹1,942.60','-0.84%']]
export default function Watchlist(){const [q,setQ]=useState(''); const [list,setList]=useState(stocks); return <main className="p-6 md:p-10 max-w-6xl mx-auto"><a href="/" className="text-sm text-cyan-400">← MarketOS</a><div className="flex flex-col md:flex-row md:items-end justify-between mt-8 gap-4"><div><div className="text-cyan-400 text-sm">WATCHLIST</div><h1 className="text-4xl font-bold mt-2">Your Watchlist</h1><p className="text-slate-500 mt-2">Track the companies you care about.</p></div><form onSubmit={e=>{e.preventDefault(); if(q.trim()&&!list.some(x=>x[0]===q.trim().toUpperCase())) setList([...list,[q.trim().toUpperCase(),'—','Added']]);setQ('')}} className="flex gap-2"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Add symbol…" className="bg-white/5 border border-white/10 rounded-xl px-4 py-2.5"/><button className="bg-cyan-400 text-black font-semibold px-4 rounded-xl">Add</button></form></div><div className="glass rounded-2xl mt-8 overflow-hidden"><div className="grid grid-cols-3 p-4 text-xs text-slate-500 border-b border-white/5"><span>ASSET</span><span>PRICE</span><span>CHANGE</span></div>{list.map(([n,p,c])=><div className="grid grid-cols-3 p-5 border-b border-white/5" key={n}><span className="font-semibold">{n}</span><span>{p}</span><span className={c[0]=='-'?'text-red-400':'text-emerald-400'}>{c}</span></div>)}</div></main>}
