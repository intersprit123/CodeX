'use client'
import {useState} from 'react'
const news=[['Markets','Global markets digest new inflation and rate expectations','MarketOS Research · 18m'],['India','Indian equities open with broad participation across sectors','MarketOS News · 42m'],['Technology','Technology shares remain in focus as investors assess growth','MarketOS Research · 1h'],['Commodities','Gold and crude oil remain key macro signals to watch','MarketOS Macro · 2h']]
export default function News(){const [filter,setFilter]=useState('All');const cats=['All','Markets','India','Technology','Commodities'];const shown=filter==='All'?news:news.filter(n=>n[0]===filter);return <main className="p-6 md:p-10 max-w-5xl mx-auto"><a href="/" className="text-sm text-cyan-400">← MarketOS</a><div className="mt-8"><div className="text-cyan-400 text-sm">NEWS INTELLIGENCE</div><h1 className="text-4xl font-bold mt-2">What matters</h1><p className="text-slate-500 mt-2">Demo headlines for the product UI. Live news will be connected later.</p></div><div className="flex gap-2 mt-7 flex-wrap">{cats.map(c=><button onClick={()=>setFilter(c)} className={`px-4 py-2 rounded-xl text-sm ${filter===c?'bg-cyan-400 text-black font-semibold':'bg-white/5 text-slate-400'}`} key={c}>{c}</button>)}</div><div className="space-y-3 mt-5">{shown.map(([c,t,m])=><article className="glass rounded-2xl p-6 hover:bg-white/[.05]" key={t}><div className="text-xs text-cyan-400">{c}</div><h2 className="text-lg font-semibold mt-2">{t}</h2><div className="text-xs text-slate-500 mt-3">{m}</div></article>)}</div></main>}
