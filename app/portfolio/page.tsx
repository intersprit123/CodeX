'use client'
import {useMemo,useState} from 'react'
type Holding=[symbol:string,quantity:number,price:number]
const seed:Holding[]=[['TCS',10,3284.4],['RELIANCE',8,1418.3],['INFY',12,1492.2]]
export default function Portfolio(){const [cash,setCash]=useState(250000);const [rows,setRows]=useState<Holding[]>(seed);const value=useMemo(()=>cash+rows.reduce((a,[,q,p])=>a+q*p,0),[cash,rows]);return <main className="p-6 md:p-10 max-w-7xl mx-auto"><a href="/" className="text-sm text-cyan-400">← MarketOS</a><div className="mt-8"><div className="text-cyan-400 text-sm">PORTFOLIO</div><h1 className="text-4xl font-bold mt-2">Portfolio Intelligence</h1><p className="text-slate-500 mt-2">Demo holdings · ready for persistent accounts later.</p></div><div className="grid md:grid-cols-4 gap-4 mt-8">{[['Total Value',`₹${Math.round(value).toLocaleString('en-IN')}`],['Today','+₹8,420'],['Total P&L','+₹84,210'],['Cash',`₹${cash.toLocaleString('en-IN')}`]].map(([a,b])=><div className="glass rounded-2xl p-5" key={a}><div className="text-xs text-slate-500">{a}</div><div className="text-2xl font-semibold mt-3">{b}</div></div>)}</div><div className="glass rounded-2xl mt-5 overflow-hidden"><div className="p-5 border-b border-white/5 flex justify-between"><h2 className="font-semibold text-lg">Holdings</h2><button onClick={()=>setRows([...rows,['NEW',1,100]])} className="text-cyan-400 text-sm">+ Add demo holding</button></div>{rows.map(([s,q,p])=><div className="grid grid-cols-3 p-5 border-b border-white/5" key={s}><span className="font-semibold">{s}</span><span>{q} shares</span><span>₹{p.toLocaleString('en-IN')}</span></div>)}</div></main>}
